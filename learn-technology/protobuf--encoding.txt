## create at 2018.1.12
## Protobuf 类型在序列化时,编码规则
了解不同protocol buffer格式化会影响编码信息的大小。

一、Base 128 Barints
	Varints 是一种使用一个或者多个字节来序列化整数的规则。数值越小,占用的字节数量越小。

variant中,除了最后一个字节，都设置了最高有效位，这表示还有更多的字节出现。
每个字节的低七位用来存储以七位组的形式的数值的二进制补码,最低有效阻首位。
